// Add interactivity or animations here if needed
// Example: Add a click event to the buttons
document.querySelectorAll('.flower-card button').forEach(button => {
    button.addEventListener('click', () => {
      alert('Added to cart!');
    });
  });